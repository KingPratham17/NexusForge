package ${packagePath};

import java.net.URISyntaxException;

import org.apache.camel.Consumer;
import org.apache.camel.Processor;
import org.apache.camel.Producer;
import org.apache.camel.spi.UriEndpoint;
import org.apache.camel.spi.UriParam;
import org.apache.camel.support.DefaultPollingEndpoint;

/**
 * SAP Cloud Integration Custom Adapter Endpoint for ${adapterName}.
 * Generated by SAP Custom Adapter Generator.
 */
@UriEndpoint(
        firstVersion = "1.0.0",
        scheme = "${scheme}",
        syntax = "${scheme}",
        title = "${adapterName}"
)
public class ${adapterClassName}Endpoint extends DefaultPollingEndpoint {

    private final ${adapterClassName}Component component;

${uriParamFields}

    public ${adapterClassName}Endpoint() {
        this.component = null;
    }

    public ${adapterClassName}Endpoint(
            final String endpointUri,
            final ${adapterClassName}Component component)
            throws URISyntaxException {

        super(endpointUri, component);
        this.component = component;
    }

    public ${adapterClassName}Endpoint(
            final String uri,
            final String remaining,
            final ${adapterClassName}Component component)
            throws URISyntaxException {

        this(uri, component);
    }

${uriParamGettersSetters}

${createProducerMethod}

${createConsumerMethod}

    @Override
    public boolean isSingleton() {
        return true;
    }
}
